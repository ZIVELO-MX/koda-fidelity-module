# FID-0003, ola 1: panel, escáner, clientes y portal

> **Para agentes ejecutores:** SUB-SKILL REQUERIDA: usar `superpowers:subagent-driven-development` o `superpowers:executing-plans` para implementar tarea por tarea. Los pasos usan casillas (`- [ ]`).

**Goal:** Llevar a código las cuatro superficies de uso diario, con el ADN visual de KODA y los estados completos, sin inventar datos que el backend todavía no entrega.

**Architecture:** Se trabaja de dentro hacia fuera. Primero los tokens y el motor de color, que gobiernan todo lo demás; después la navegación por rol; después cada superficie. Las pruebas son de componente con vitest y testing-library, que es el patrón que el repositorio ya usa, más un recorrido de Playwright al final. Nada de backend: donde el dato no existe, la interfaz muestra su estado honesto y se deja la casilla sin marcar.

**Tech Stack:** Next 16, React 19, Tailwind v4 con `@theme inline`, vitest con jsdom, testing-library, Playwright, Recharts ya instalado.

**Spec:** `zivelo-knowledge/plans/2026-09-20-fidelity/design.md`, más `docs/design/design-system.md`, `docs/design/contratos-de-interfaz.md` y `docs/design/wireframe-rediseno.html` de este repositorio.

## Global Constraints

- Rama `1.2.0/rulaxx`, sale de `dev` y apunta a `dev`. Traer `dev` antes de cada push. **Nunca mergear.**
- Tipografía: Switzer de Fontshare para interfaz, JetBrains Mono con `tabular-nums` para cifras en columna.
- Acento de KODA `#FF6B35`, con `hover #E8552B`, `pressed #CC4719`, `soft #FFF1E9`, `ink #5A1F08`.
- Grises cálidos: `#FAFAF7`, `#F5F4EE`, `#FFFFFF`, `#F7F6F1`, bordes `#EBE9E0` y `#D9D6C9`, tinta `#1C1B17`, `#5E5A51`, `#8E897C`, `#B5B0A4`.
- Radios 4, 6, 8, 10, 14, 18, 24 y 999. Sombras cálidas sobre `rgba(28,27,23,…)`.
- Área táctil mínima 40px, y 44px en destinos de navegación.
- Foco `outline: 2px solid` del acento con `offset: 2px`, con contorno interior de apoyo sobre superficies claras. Nunca se elimina.
- Contraste AA: 4.5:1 en texto normal, 3:1 en texto grande y elementos de interfaz.
- Movimiento: presión 120ms, sellos 200ms desde `scale(.92)`, menús 180ms, hoja móvil 320ms, cifras 300ms escalonadas 60ms sin rebote. Teclado no se anima. Todo respeta `prefers-reduced-motion`.
- Cero em dash en texto visible.
- **Ninguna cifra que el backend no calcule bien se muestra como buena.** Se marca como parcial y se explica.
- La forma de la tarjeta no cambia. Los temas son piel sobre `components/loyalty-card-preview.tsx`.

## Bloqueos conocidos

Estos criterios de `FID-0003` **no se pueden cerrar en esta ola** y su casilla se queda sin marcar, con la razón escrita:

| Criterio | Bloqueado por |
|---|---|
| Consumir métricas históricas, serie diaria, actividad paginada y top de tarjetas | El endpoint extendido no existe. `FID-0006` |
| Representar `redemptionRate: null` | El campo no existe todavía. `FID-0006` |
| Mostrar errores tipados y `requestId` | El catálogo no está implementado. `FID-0006` |
| Verificar navegación del rol sellador de punta a punta | El sellador no puede entrar. `FID-0004`, bug B1 |
| Tarjeta desactivada por cambio de plan | `CardStatus` no existe. `FID-0007` |

Lo que sí entra: la interfaz de todos ellos, con su estado de dato parcial, vacío o pendiente, lista para consumir el contrato cuando llegue.

---

## File Structure

| Archivo | Responsabilidad |
|---|---|
| `app/globals.css` | Tokens del ADN, claro y oscuro. Es la raíz de todo lo visual |
| `app/layout.tsx` | Carga de Switzer y JetBrains Mono |
| `lib/color-marca.ts` | Nuevo. Deriva estados y color de texto del color del negocio, con clamp |
| `lib/__tests__/color-marca.test.ts` | Nuevo. Casos límite del motor de color |
| `app/dashboard/(main)/layout.tsx` | Aplica las variables derivadas en vez del color crudo |
| `components/dashboard/sidebar.tsx` | Navegación agrupada por rol, sin escáner en escritorio |
| `app/dashboard/(main)/page.tsx` | Panel reorganizado, y arreglo del redirect tragado y del enlace de documentación |
| `app/dashboard/scan/page.tsx` | Escáner de una sola acción |
| `components/dashboard/customers-table.tsx` | Sellar desde la fila y fila de quien completó |
| `app/dashboard/(main)/customers/page.tsx` | Filtro de listos para canjear |
| `app/dashboard/my-cards/page.tsx` | Portal del cliente |
| `e2e/ola-1.spec.ts` | Nuevo. Recorrido en 375, 768 y 1440 |

---

## Task 1: Tokens del ADN de KODA

**Files:**
- Modify: `app/globals.css`
- Modify: `app/layout.tsx`

**Interfaces:**
- Produces: las variables CSS que consumen todas las tareas siguientes.

- [x] **Step 1: Sustituir la escala neutra por los grises cálidos**

En `app/globals.css`, dentro de `:root`, reemplazar los valores `oklch` neutros por los del ADN, en hexadecimal:

```css
:root {
  --background: #FAFAF7;
  --foreground: #1C1B17;
  --card: #FFFFFF;
  --card-foreground: #1C1B17;
  --muted: #F7F6F1;
  --muted-foreground: #5E5A51;
  --border: #EBE9E0;
  --input: #D9D6C9;
  --primary: #FF6B35;
  --primary-foreground: #FFFFFF;
  --ring: #FF6B35;
  --radius: 0.875rem;
}
```

- [x] **Step 2: Corregir el modo oscuro, que hoy es azulado**

En el bloque `.dark`, sustituir los `oklch(... 260)` por los valores cálidos del ADN:

```css
.dark {
  --background: #0D0C0A;
  --foreground: #FAFAF7;
  --card: #1A1814;
  --card-foreground: #FAFAF7;
  --muted: #221F1A;
  --muted-foreground: #CBC6B9;
  --border: #2F2B25;
  --input: #3D3830;
  --primary: #FF7A47;
  --primary-foreground: #0D0C0A;
  --ring: #FF7A47;
}
```

- [x] **Step 3: Cargar las dos tipografías**

En `app/layout.tsx`, sustituir la importación de Geist. Switzer no está en Google Fonts, así que se carga de Fontshare con `next/font/local` o con un `<link>` a `api.fontshare.com`. JetBrains Mono sí está en Google Fonts:

```tsx
import { JetBrains_Mono } from 'next/font/google'

const mono = JetBrains_Mono({
  subsets: ['latin'],
  variable: '--font-mono',
  display: 'swap',
})
```

Y en `app/globals.css`, dentro de `@theme inline`:

```css
--font-sans: 'Switzer', 'Söhne', -apple-system, BlinkMacSystemFont, 'Segoe UI', ui-sans-serif;
--font-mono: var(--font-mono), ui-monospace, SFMono-Regular, Menlo, monospace;
```

- [x] **Step 4: Foco global con contorno de apoyo**

Añadir al final de `app/globals.css`:

```css
:focus-visible {
  outline: 2px solid var(--ring);
  outline-offset: 2px;
  box-shadow: 0 0 0 1px rgba(28, 27, 23, 0.55);
}
```

El `box-shadow` interior existe porque el acento sobre superficie clara se queda en 2.84 de contraste, por debajo del mínimo de 3. Medido en `docs/design/verificadores/tokens-resultado.md`.

- [x] **Step 5: Comprobar que compila y que nada se rompió**

```bash
npx tsc --noEmit 2>&1 | grep -v "scripts/create-" | tail -3
npx vitest run 2>&1 | tail -3
```

Esperado: los únicos errores de `tsc` son los preexistentes de `scripts/create-client.ts` y `scripts/create-user.ts`. Las pruebas siguen pasando.

- [x] **Step 6: Commit**

```bash
git add app/globals.css app/layout.tsx
git commit -m "feat: adopt KODA design tokens and typography"
```

---

## Task 2: Motor de color de marca

**Files:**
- Create: `lib/color-marca.ts`
- Create: `lib/__tests__/color-marca.test.ts`
- Modify: `app/dashboard/(main)/layout.tsx`

**Interfaces:**
- Produces: `derivarMarca(hex: string): { base, hover, pressed, soft, ink, texto }`, consumido por el layout del panel.

- [x] **Step 1: Escribir la prueba que falla**

> **Desviación al ejecutar, 2026-09-05.** El primer caso de este bloque esperaba blanco sobre
> `#FF6B35`, y contradice tanto al motor de abajo como al design system, que manda calcular el
> texto por contraste y bajar a `--ink` cuando el blanco no llega a 4.5:1. El blanco sobre el
> acento de KODA da 2.84:1, el mismo par que `verificadores/tokens-resultado.md` ya registró como
> hallazgo del ADN. La prueba escrita espera tinta, y se añadieron los casos límite que el design
> system exige y aquí faltaban: negro, pastel muy claro y gris. Once casos en total.

```ts
// lib/__tests__/color-marca.test.ts
import { describe, expect, it } from "vitest"
import { derivarMarca } from "../color-marca"

describe("derivarMarca", () => {
  it("usa texto blanco sobre un naranja de marca", () => {
    expect(derivarMarca("#FF6B35").texto).toBe("#FFFFFF")
  })

  it("usa tinta oscura sobre un amarillo, donde el blanco sería ilegible", () => {
    expect(derivarMarca("#FFFF00").texto).toBe("#1C1B17")
  })

  it("aclara el estado hover menos cuando el color ya es oscuro", () => {
    const claro = derivarMarca("#FF6B35")
    const oscuro = derivarMarca("#1A1A1A")
    expect(claro.hover).not.toBe(claro.base)
    expect(oscuro.hover).not.toBe(oscuro.base)
  })

  it("acerca al rango usable un color casi blanco", () => {
    expect(derivarMarca("#FFFFFF").base).not.toBe("#FFFFFF")
  })

  it("devuelve el acento de KODA cuando el valor es inválido", () => {
    expect(derivarMarca("no es un color").base).toBe("#FF6B35")
  })
})
```

- [x] **Step 2: Ejecutarla y verla fallar**

```bash
npx vitest run lib/__tests__/color-marca.test.ts
```

Esperado: falla porque `../color-marca` no existe.

- [x] **Step 3: Escribir el motor**

```ts
// lib/color-marca.ts
// El color del negocio llega de un selector libre, así que no se inyecta crudo.
// De él se derivan los estados y el color de texto, calculado por contraste.

const ACENTO_KODA = "#FF6B35"
const TINTA = "#1C1B17"
const BLANCO = "#FFFFFF"

export type Marca = {
  base: string
  hover: string
  pressed: string
  soft: string
  ink: string
  texto: string
}

function normalizar(hex: string): string | null {
  const limpio = hex.trim().replace(/^#/, "")
  if (/^[0-9a-fA-F]{3}$/.test(limpio)) {
    return "#" + limpio.split("").map((c) => c + c).join("").toUpperCase()
  }
  if (/^[0-9a-fA-F]{6}$/.test(limpio)) return "#" + limpio.toUpperCase()
  return null
}

function canales(hex: string): [number, number, number] {
  const n = hex.slice(1)
  return [0, 2, 4].map((i) => parseInt(n.slice(i, i + 2), 16)) as [number, number, number]
}

function aHex(c: number[]): string {
  return "#" + c.map((v) => Math.max(0, Math.min(255, Math.round(v))).toString(16).padStart(2, "0")).join("").toUpperCase()
}

function canalLineal(v: number): number {
  const s = v / 255
  return s <= 0.03928 ? s / 12.92 : Math.pow((s + 0.055) / 1.055, 2.4)
}

export function luminancia(hex: string): number {
  const [r, g, b] = canales(hex)
  return 0.2126 * canalLineal(r) + 0.7152 * canalLineal(g) + 0.0722 * canalLineal(b)
}

export function contraste(a: string, b: string): number {
  const la = luminancia(a)
  const lb = luminancia(b)
  const [alto, bajo] = la > lb ? [la, lb] : [lb, la]
  return (alto + 0.05) / (bajo + 0.05)
}

function mezclar(hex: string, destino: number, p: number): string {
  return aHex(canales(hex).map((c) => c + (destino - c) * p))
}

export function derivarMarca(entrada: string): Marca {
  const hex = normalizar(entrada) ?? ACENTO_KODA

  // Un color casi blanco o casi negro se acerca al rango usable, para que el
  // foco y los botones sigan viéndose.
  let base = hex
  const l = luminancia(hex)
  if (l > 0.75) base = mezclar(hex, 0, 0.35)
  else if (l < 0.03) base = mezclar(hex, 255, 0.2)

  // Un tono ya oscuro se oscurece menos: el ajuste es proporcional.
  const lb = luminancia(base)
  const paso = 0.12 + lb * 0.18

  return {
    base,
    hover: mezclar(base, 0, paso),
    pressed: mezclar(base, 0, paso * 2),
    soft: mezclar(base, 255, 0.88),
    ink: mezclar(base, 0, 0.55),
    texto: contraste(BLANCO, base) >= 4.5 ? BLANCO : TINTA,
  }
}
```

- [x] **Step 4: Ejecutar la prueba y verla pasar**

```bash
npx vitest run lib/__tests__/color-marca.test.ts
```

Esperado: los cinco casos pasan.

- [x] **Step 5: Consumirlo en el layout del panel**

En `app/dashboard/(main)/layout.tsx`, sustituir la inyección cruda:

```tsx
import { derivarMarca } from "@/lib/color-marca"

const marca = derivarMarca(business.brandColor)
```

y el `style` del contenedor:

```tsx
style={{
  '--primary': marca.base,
  '--primary-foreground': marca.texto,
  '--ring': marca.base,
  '--sidebar-primary': marca.base,
  '--sidebar-primary-foreground': marca.texto,
  '--chart-1': marca.base,
} as React.CSSProperties}
```

- [x] **Step 6: Verificar**

```bash
npx vitest run 2>&1 | tail -3
npx tsc --noEmit 2>&1 | grep -v "scripts/create-" | tail -3
```

- [x] **Step 7: Commit**

```bash
git add lib/color-marca.ts lib/__tests__/color-marca.test.ts "app/dashboard/(main)/layout.tsx"
git commit -m "feat: derive brand color states with contrast-aware text"
```

---

## Task 3: Navegación agrupada por rol

**Files:**
- Modify: `components/dashboard/sidebar.tsx`
- Modify: `components/dashboard/__tests__/sidebar-roles.test.tsx`

**Interfaces:**
- Consumes: los tokens de la Task 1.
- Produces: la navegación que las superficies siguientes asumen.

- [x] **Step 1: Escribir las pruebas que fallan**

> **Desviaciones al ejecutar, 2026-09-05.**
>
> 1. La tercera prueba, tal cual, falla incluso con la navegación correcta: el botón flotante del
>    escáner es un enlace con nombre accesible "Abrir escáner", que también casa con `/escáner/i`.
>    Se acotó la aserción al `aside`, que es la navegación de escritorio, para no borrar el botón
>    que el propio plan manda conservar.
> 2. Reagrupar invalidó seis pruebas existentes de este archivo, que el plan no menciona: el
>    destino pasó de "Tarjetas de Lealtad" a "Tarjetas", y el sellador dejó de tener Tarjetas,
>    Códigos QR y el escáner en su mapa. Se actualizaron a la intención nueva.
> 3. La barra móvil no estaba acotada por rol y quedaba en contradicción con el aside: el sellador
>    veía "Tarjetas" abajo aunque su rol ya no administra tarjetas. Se acotó igual que el aside.
> 4. `openGroups` guardaba en `localStorage` las etiquetas anteriores. Sin descartarlas, el
>    acordeón abría vacío tras el cambio de nombres. Haber cerrado los grupos a propósito sí se
>    respeta.
> 5. `docs/page.tsx` decía "Ve a la sección 'Tarjetas de Lealtad' en el panel lateral". Se corrigió
>    al nombre y al grupo nuevos, porque este cambio la dejó mintiendo.

Añadir a `components/dashboard/__tests__/sidebar-roles.test.tsx`:

```tsx
it("agrupa los destinos en Operación, Programa y Negocio", () => {
  render(<DashboardSidebar {...props} role="admin" />)
  expect(screen.getByText("Operación")).toBeInTheDocument()
  expect(screen.getByText("Programa")).toBeInTheDocument()
  expect(screen.getByText("Negocio")).toBeInTheDocument()
})

it("el sellador solo ve Operación", () => {
  render(<DashboardSidebar {...props} role="sellador" />)
  expect(screen.getByText("Operación")).toBeInTheDocument()
  expect(screen.queryByText("Negocio")).not.toBeInTheDocument()
})

it("no ofrece el escáner en la navegación de escritorio", () => {
  render(<DashboardSidebar {...props} role="admin" />)
  expect(screen.queryByRole("link", { name: /escáner/i })).not.toBeInTheDocument()
})
```

- [x] **Step 2: Ejecutarlas y verlas fallar**

```bash
npx vitest run components/dashboard/__tests__/sidebar-roles.test.tsx
```

- [x] **Step 3: Reagrupar `navGroups`**

```tsx
const navGroups = [
  {
    label: "Operación",
    roles: ["admin", "sellador"] as Role[],
    items: [
      { name: "Panel", href: "/dashboard", icon: LayoutDashboard },
      { name: "Clientes", href: "/dashboard/customers", icon: Users },
    ],
  },
  {
    label: "Programa",
    roles: ["admin"] as Role[],
    items: [
      { name: "Tarjetas", href: "/dashboard/cards", icon: CreditCard },
      { name: "Códigos QR", href: "/dashboard/qr-codes", icon: QrCode },
    ],
  },
  {
    label: "Negocio",
    roles: ["admin"] as Role[],
    items: [
      { name: "Marca", href: "/dashboard/branding", icon: Palette },
      { name: "Equipo", href: "/dashboard/team", icon: UserCog },
      { name: "Configuración", href: "/dashboard/settings", icon: Settings },
      { name: "Documentación", href: "/dashboard/docs", icon: BookOpen },
    ],
  },
]
```

Retirar la línea que añade el escáner a la navegación de escritorio. **El botón flotante móvil se conserva**, porque ahí sí es la acción central.

- [x] **Step 4: Ejecutar y ver pasar**

```bash
npx vitest run components/dashboard/__tests__/sidebar-roles.test.tsx
```

- [x] **Step 5: Área táctil de 44px en cada destino**

En el `className` del ítem de navegación, sustituir el relleno actual por `min-h-11` con el relleno horizontal existente.

- [x] **Step 6: Commit**

```bash
git add components/dashboard/sidebar.tsx components/dashboard/__tests__/sidebar-roles.test.tsx
git commit -m "feat: group dashboard navigation by role"
```

---

## Task 4: Panel reorganizado

**Files:**
- Modify: `app/dashboard/(main)/page.tsx`

**Interfaces:**
- Consumes: Tasks 1 a 3.

- [x] **Step 1: Sacar los `redirect()` del `try`**

Hoy la página envuelve la carga en `try { … } catch { loadingError = true }` y dentro llama `redirect()`, que funciona lanzando `NEXT_REDIRECT`. El `catch` se lo traga y pinta "Error al cargar el panel" en vez de redirigir.

Resolver la sesión y el negocio **antes** del `try`, y dejar dentro solo las consultas de datos.

- [x] **Step 2: Corregir el enlace móvil de documentación**

Cambiar `href="/docs"` por `href="/dashboard/docs"`. La primera ruta no existe.

- [x] **Step 3: Sustituir las cuatro cifras por las tres del día**

Las cifras actuales no son ciertas: "Canjes" cuenta solo entre los últimos diez eventos, y "Sellos entregados" suma el saldo, que baja con cada canje. Mientras `FID-0006` no entregue los agregados:

- Mostrar sellos, canjes y clientes nuevos **de hoy**, derivados de `StampLog` con `createdAt` del día, que sí es correcto con los datos actuales.
- Cada cifra con su comparación contra ayer.
- La tendencia de 30 días se muestra como sección vacía con su explicación, **no con datos inventados**.

> **Precisiones al ejecutar, 2026-09-05.**
>
> 1. "El día" no se puede cortar en UTC. El servidor corre en UTC y las cifras de hoy se
>    reiniciarían a las seis de la tarde, en plena hora pico de un negocio mexicano. Se añadió
>    `lib/dia-local.ts`, que corta el día en `America/Mexico_City`, con sus pruebas. La zona está
>    fija hasta que `Business` tenga la suya.
> 2. Los clientes nuevos no salen de `StampLog`, que no registra altas. Salen de `Customer` por
>    `createdAt`.
> 3. La comparación contra ayer corta ayer **a la misma hora del día**. Contra el día completo de
>    ayer, un panel abierto por la mañana diría "menos que ayer" siempre, que es una cifra que
>    engaña aunque cada número por separado sea cierto.

- [x] **Step 4: Añadir el bloque de atención**

Clientes a un sello de completar y tarjetas por vencer, en ámbar de aviso y no en rojo, derivados de datos que la página ya recibe.

- [x] **Step 5: Actividad en lista**

Sustituir el carrusel horizontal por una lista con la hora a la derecha en mono tabular. Retirar el bloque de Wallet, que promete una función apagada.

- [x] **Step 6: Verificar**

```bash
npx tsc --noEmit 2>&1 | grep -v "scripts/create-" | tail -3
npx vitest run 2>&1 | tail -3
```

- [x] **Step 7: Commit**

```bash
git add "app/dashboard/(main)/page.tsx"
git commit -m "feat: rebuild the dashboard around today and attention"
```

---

## Task 5: Escáner de una sola acción

**Files:**
- Modify: `app/dashboard/scan/page.tsx`

- [x] **Step 1: Abrir la cámara al entrar**

Hoy hay un botón que alterna `useCamera`. Iniciar con la cámara activa y dejar el botón solo para apagarla o cuando el permiso esté denegado.

- [x] **Step 2: Búsqueda siempre visible**

La búsqueda por nombre deja de ser una alternativa escondida y vive bajo el visor.

- [x] **Step 3: Una sola acción primaria**

Sellar es sólido y canjear es contorno. Cuando la tarjeta está completa se invierten, y sellar se deshabilita con su razón visible.

- [x] **Step 4: Resultado en la propia pantalla**

Retirar el diálogo bloqueante del bono sorpresa y anunciarlo sobre la pantalla, con la transición de 200ms desde `scale(.92)`.

- [x] **Step 5: Área táctil de 48px en la acción principal**

- [x] **Step 6: Verificar y commit**

```bash
npx vitest run components/scan/__tests__/qr-scanner.test.tsx
git add app/dashboard/scan/page.tsx
git commit -m "feat: rebuild the scanner around a single primary action"
```

---

## Task 6: Clientes, sellar desde la fila

**Files:**
- Modify: `components/dashboard/customers-table.tsx`
- Modify: `components/dashboard/__tests__/customers-table.test.tsx`
- Modify: `app/dashboard/(main)/customers/page.tsx`

- [x] **Step 1: Escribir las pruebas que fallan**

```tsx
it("ofrece sellar en la propia fila, sin abrir un menú", () => {
  render(<CustomersTable customers={[clienteConTresSellos]} {...props} />)
  expect(screen.getByRole("button", { name: /sellar/i })).toBeVisible()
})

it("cambia la acción a canjear cuando la tarjeta está completa", () => {
  render(<CustomersTable customers={[clienteCompleto]} {...props} />)
  expect(screen.getByRole("button", { name: /canjear/i })).toBeVisible()
})
```

- [x] **Step 2: Ejecutarlas y verlas fallar**

```bash
npx vitest run components/dashboard/__tests__/customers-table.test.tsx
```

- [x] **Step 3: Implementar la acción en la fila**

Sacar sellar y canjear del menú de tres puntos. La fila de quien completó lleva fondo de éxito. El resto de acciones se quedan en el menú.

- [x] **Step 4: Filtro de listos para canjear**

En `customers/page.tsx`, añadir el filtro derivado de `stamps >= stampsRequired`, con los datos que la página ya recibe.

- [x] **Step 5: Ejecutar, verificar y commit**

```bash
npx vitest run components/dashboard/__tests__/customers-table.test.tsx
git add components/dashboard/customers-table.tsx components/dashboard/__tests__/customers-table.test.tsx "app/dashboard/(main)/customers/page.tsx"
git commit -m "feat: stamp and redeem from the customer row"
```

---

## Task 7: Portal del cliente

**Files:**
- Modify: `app/dashboard/my-cards/page.tsx`

- [x] **Step 1: La tarjeta a pantalla**

Sustituir la fila resumida por `LoyaltyCardPreview` a tamaño completo con su QR.

- [x] **Step 2: Lo que falta, en palabras**

Bajo cada tarjeta, "Te faltan N sellos" además de la fracción.

- [x] **Step 3: Orden por cercanía al premio**

Ordenar por `stampsRequired - stamps` ascendente, agrupado por negocio.

- [x] **Step 4: Vencidas colapsadas**

Al fondo, en gris y con su conteo. Nunca en rojo: que una tarjeta venza no es un error de la persona.

- [x] **Step 5: Retirar los tres botones apagados**

Los que dicen "Próximamente". Los de Wallet vuelven cuando pase su gate; descargar y eliminar pertenecen al borrado del cliente y se resuelven en su propia ola.

- [x] **Step 6: Verificar y commit**

```bash
npx tsc --noEmit 2>&1 | grep -v "scripts/create-" | tail -3
git add app/dashboard/my-cards/page.tsx
git commit -m "feat: give the customer portal its card back"
```

---

## Task 8: Verificación y entrega

**Files:**
- Create: `e2e/ola-1.spec.ts`

- [x] **Step 1: Recorrido en tres anchos**

Escribir el recorrido del panel y sus estados compartidos en 375, 768 y 1440, siguiendo el patrón de los specs que ya existen en `e2e/`.

> **Límite al ejecutar, 2026-09-05.** El recorrido cubre las rutas alcanzables sin sesión, en los
> tres anchos: 18 comprobaciones en verde. **Las cuatro superficies de la ola no se pudieron
> recorrer**, porque viven detrás del login y este repositorio no tiene sesión sembrada para
> pruebas. La regresión del panel queda escrita pero omitida en esta máquina: solo hay
> `.env.example`, y el proxy construye su cliente de Supabase antes de que la ruta responda. Con
> credenciales, esa prueba corre sola.

- [x] **Step 2: Comprobar que no hay desbordamiento horizontal ni áreas táctiles por debajo de 44px**

Reutilizar las dos comprobaciones de `docs/design/verificadores/entrega.spec.ts`, que ya están escritas y probadas.

- [x] **Step 3: Verificar que las rutas sin confirmar no recibieron rediseño**

```bash
git diff --name-only origin/dev...HEAD | grep -E "app/(login|signup|invite)|auth/error|update-password|forbidden" && echo "FALLO: se tocó una pantalla bloqueada" || echo "ok"
```

- [x] **Step 4: Suite completa**

```bash
npx vitest run 2>&1 | tail -3
npx eslint . 2>&1 | tail -2
npx tsc --noEmit 2>&1 | grep -v "scripts/create-" | tail -3
```

- [ ] **Step 5: Empujar con `dev` al día y abrir el PR**

```bash
git fetch origin && git merge --ff-only origin/dev
git push
```

El PR queda listo para revisión. **El merge lo decide el usuario.**

> **Estado al 2026-09-05.** `origin/dev` no se había movido, así que el `merge --ff-only` no
> trajo nada. La rama está empujada, 36 commits por delante de `dev`. **El PR no se pudo abrir:**
> `gh` no está autenticado en esta máquina. El cuerpo del PR queda redactado y listo para pegar.
>
> Además, la misión `FID-0003` dice rama `rulaxx/1.2.0` y prohíbe `dev` como base y destino,
> mientras que estas restricciones globales dicen `1.2.0/rulaxx` saliendo de `dev` y apuntando a
> `dev`. La rama real del repositorio es `1.2.0/rulaxx`. Falta decidir cuál regla manda antes de
> abrir el PR.

- [x] **Step 6: Marcar en TLOZ solo lo que tenga evidencia**

Las cinco casillas de la tabla de bloqueos se quedan sin marcar, con su razón.

> **Hecho y verificado, 2026-09-05.** `FID-0003` queda en 11 de 19, 58 por ciento, con la sección
> de estado escrita en el documento. Sin marcar: los cuatro criterios bloqueados por backend, más
> accesibilidad AA de punta a punta, el recorrido de Playwright sobre las superficies con sesión
> y el PR adjunto. Cada uno con su razón en el documento de la misión.

---

## Self-Review

**Cobertura.** Los diez criterios de cierre originales y los nueve de superficies añadidas quedan cubiertos así: tokens y ADN en la Task 1, motor de color en la 2, navegación por rol en la 3, panel y sus dos bugs en la 4, escáner en la 5, clientes en la 6, portal en la 7, y verificación en la 8. Cinco criterios quedan bloqueados y su razón está en la tabla de arriba, no escondida.

**Marcadores de relleno.** Las Tasks 1, 2, 3 y 6 traen el código completo. Las Tasks 4, 5 y 7 describen cambios sobre archivos existentes que hay que leer antes de editar, con la instrucción exacta de qué sustituir y por qué; no se transcribe el archivo entero porque supera las trescientas líneas.

**Consistencia de nombres.** `derivarMarca` de la Task 2 se consume con ese nombre en el layout. Los tokens de la Task 1 son los que citan las demás. Los grupos de navegación de la Task 3 son los que asume la Task 4.

**Riesgo conocido.** El escáner y la lista de clientes son pantallas del sellador, y el sellador no puede entrar hasta que `FID-0004` cierre el bug B1. Se construyen y se verifican con una cuenta de administrador; la verificación con el rol real queda pendiente y su casilla sin marcar.
